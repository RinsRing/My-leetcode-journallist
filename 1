这是第一题，使用暴力求解
    
    vector<int> twoSum(vector<int>& nums, int target) 
    {
        int n=nums.size();
        for(int i=0;i<n;i++)
        {
            for(int j=i+1;j<n;j++)
            if(nums[i]+nums[j]==target)
            {
                return {i,j};
            }
        }
        return {};
    }

还可以使用哈希表解，目前不会哈希表，记下来
